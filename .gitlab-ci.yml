image: node:18

pages:
  script:
    - npm install
    - npm run build
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - main  # nebo jiná větev, ze které nasazuješ
    - project-start
